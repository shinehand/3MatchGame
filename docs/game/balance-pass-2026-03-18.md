# Balance Pass 2026-03-18

## 현재 상태

- `001-100` 스테이지 JSON이 모두 로드된다.
- `validate_stage_data.sh` 구조 검증 통과
- `validate_stage_balance.sh` 밴드 규칙 검증 통과
- `validate_gameplay.sh` 통합 검증 통과

## 자동 검증 요약

- `1-10`: moves `12-13`, blockers `0-2`, active_cells `48-64`
- `11-20`: moves `13-14`, blockers `2-4`, active_cells `40-52`
- `21-30`: moves `14-15`, blockers `4-6`, active_cells `40-52`
- `31-40`: moves `15-16`, blockers `6-7`, active_cells `44-52`
- `41-50`: moves `16-17`, blockers `8-9`, active_cells `44-52`
- `51-60`: moves `16-17`, blockers `9-10`, active_cells `44-52`
- `61-70`: moves `17-18`, blockers `10-11`, active_cells `44-52`
- `71-80`: moves `18`, blockers `11-12`, active_cells `44-52`
- `81-90`: moves `18`, blockers `12-13`, active_cells `44-52`
- `91-100`: moves `18-19`, blockers `13-14`, active_cells `44-52`

## 1차 판단

- 밴드별 `이동 수`와 `덤불 수` 증가는 현재 기획 문서의 곡선과 맞는다.
- 초반 `1-10`은 활성 칸 편차가 크므로 실제 체감 난이도 차이가 가장 클 가능성이 있다.
- 중반 `31-60`은 활성 칸 수는 안정적이지만 덤불 수가 빠르게 증가하므로 첫 답답함 구간이 될 가능성이 높다.
- 후반 `71-100`은 이동 수 증가폭이 작기 때문에 실제 난이도는 `보드 마스크 읽기`에 크게 좌우된다.

## 수동 플레이 우선 구간

1. `Stage 8-10`
2. `Stage 31-35`
3. `Stage 61-65`
4. `Stage 91-95`

## 수동 플레이 때 볼 기준

- 첫 3수 안에 목표가 읽히는지
- 첫 특수 블록이 너무 늦게 나오지 않는지
- 실패가 억울한지, 납득 가능한지
- 덤불이 전략 요소인지 단순 막힘인지
- 보드 모양이 재미를 만드는지 불편함만 만드는지

## 다음 조정 원칙

- 초반은 `성공률` 우선
- 중반은 `특수 블록 활용` 우선
- 후반은 `복합 목표 압박` 우선
- 새 메커닉 추가 없이 `이동 수`, `덤불 수`, `가중치`, `보드 마스크`만 조정

## 1차 수치 조정 완료

- `Stage 8-10`
  - 점수 목표 완화
  - 토끼/병아리 목표 수치와 출현 가중치 보정
- `Stage 31-35`
  - `31-34` 이동 수를 `16`으로 상향
  - 수집 목표 수치와 점수 목표를 소폭 완화
  - `Stage 35` 개구리 수집량을 줄이고 개구리 출현 가중치를 상향
- `Stage 61-65`
  - `61-64` 이동 수를 `18`로 상향
  - 점수전 수치 완화
  - 이중/삼중 수집량을 한 단계 완화
- `Stage 91-95`
  - `91-95` 이동 수를 `19`로 상향
  - 수집 및 점수 목표를 소폭 완화
  - `Stage 94` 덤불을 1개 줄여 첫 판 막힘 확률 완화

## 2차 수치 조정 완료

- `Stage 41-50`
  - `41-44` 이동 수를 `17`로 상향
  - 점수 중심 스테이지의 목표 점수를 완화
  - 수집 목표를 1단계 줄이고 목표 동물 가중치를 보강
  - `48`, `50`은 복합 목표 압박을 소폭 완화
- `Stage 51-60`
  - `51-54` 이동 수를 `17`로 상향
  - `55-60`은 이동 수를 유지하고 수집/점수 목표만 완화
  - 삼중 수집 구간은 목표 수를 `5 -> 4`로 내려 첫 특수 블록 체감 전 실패 확률을 완화
- `Stage 96-100`
  - `96` 이동 수를 `19`로 상향하고 덤불을 1개 줄임
  - `97-99`는 최종권 목표 수치와 점수를 한 단계 완화
  - `100`은 수집 목표를 `8 x 4종 -> 6 x 4종`, 점수 목표를 `7000 -> 6200`, 덤불을 `14 -> 13`으로 조정
