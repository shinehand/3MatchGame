# Task Brief: Development Team Split 2026-03-23

## 작업명

- 기존 태스크 기반 팀별 개발 분담 실행

## 목표

- `.codex/tasks/flow-dev-2026-03-22.md`, `.codex/tasks/stage-100-rollout.md`, `.codex/tasks/art-productization-alpha-2026-03-19.md`에 정의된 항목을 팀별로 분담해 실행한다.

## 팀별 분담

### Development Lead

- 파일 소유 범위를 정하고 충돌 없이 병렬 실행 가능한 단위로 쪼갠다.
- P3 완료 후 QA팀 핸드오프를 결정한다.

### Core Developer (담당: `gameplay.gd`)

- **P3-A: 디버그 버튼 핸들러 제거**
  - `_on_shuffle_button_pressed()` 함수 제거
  - `_on_demo_match_button_pressed()` 함수 제거
  - `secondary_buttons` @onready 변수 제거
  - 레이아웃 함수 내 `secondary_buttons` 참조 두 곳 제거

### UI Developer (담당: `gameplay.tscn`)

- **P3-B: 디버그 노드 씬에서 제거**
  - `SecondaryButtons` 컨테이너 노드 제거 (ShuffleButton, DemoMatchButton 포함)
  - DemoMatchButton signal connection 제거

### Data Tools Developer (담당: `data/stages/`, `stage_catalog.gd`)

- **이번 라운드 범위 없음** — 100스테이지 JSON 데이터 및 카탈로그 로더 완료 상태 유지

### Build Integration Developer

- **이번 라운드 범위 없음** — 현재 빌드 파이프라인 안정 유지

### Code Reviewer

- Core Developer와 UI Developer 작업 완료 후 즉시 리뷰
- 확인 포인트:
  - `secondary_buttons` 참조 잔존 여부
  - 씬 노드 경로 불일치 여부
  - 신규 기능 없이 제거만 이루어졌는지

## 완료 조건

- `gameplay.tscn`에 ShuffleButton, DemoMatchButton, SecondaryButtons 노드가 없다.
- `gameplay.gd`에 `_on_shuffle_button_pressed`, `_on_demo_match_button_pressed`, `secondary_buttons` 참조가 없다.
- `./scripts/validate_gameplay.sh` 통과
- 씬 열기 시 `@onready` 노드 경로 오류 없음

## 영향 파일

- `scenes/gameplay.tscn`
- `scripts/gameplay.gd`

## QA 핸드오프 조건

- 위 완료 조건 충족 시 QA팀에 전달
- QA팀은 인게임 진입 → 플레이 → 클리어/실패 흐름에서 디버그 UI가 노출되지 않음을 확인
